{
  "name": "us-address-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:us-data": "node scripts/build-us-address-data.mjs",
    "dev": "astro dev",
    "build": "rimraf dist && astro build && node scripts/postbuild-cloudflare.mjs",
    "preview": "astro preview",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.4.2"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "playwright": "^1.58.2",
    "rimraf": "^6.1.3",
    "wrangler": "^4.78.0"
  }
}
